// Copy this file to config.js and fill in your Azure identifiers.
// These are NOT secrets — they are just identifiers for pre-filling the SSO form.
window.AZURE_CONFIG = {
  subscriptionId: "your-subscription-id",
  tenantId:       "your-tenant-id",
  clientId:       "your-app-registration-client-id",
};
